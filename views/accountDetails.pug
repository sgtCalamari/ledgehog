//- page to display specific account transactions and other details
extends layout.pug

block content
  div.d-flex.justify-content-between
    h1
      span: a(href="/"): i.bi.bi-arrow-left-circle
      |  #{accountName} Summary
    a(href="/Logout"): button.btn.btn-outline-primary.btn-header Logout
  hr
  div.d-flex.justify-content-between
    h4 Transactions
    a(href="/AccountDetails/" + accountId + "/CreateTransaction"): button.btn.btn-primary: span.bi.bi-plus New Transaction
  table.table.table-striped.table-responsive.table-hover
    thead
      tr
        th Date
        th Description
        th: a(href="/TransactionCategories") Category
        th Amount
    tbody
      if transactions
        each tx in transactions
          tr
            td: a(href="/AccountDetails/"+accountId+"/ModifyTransaction/"+tx.id) #{tx.date}
            td: strong #{tx.description}
            td #{tx.category}
            td $#{tx.amount}
