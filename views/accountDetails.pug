//- page to display specific account transactions and other details
extends layout.pug

block content
  div.d-flex.justify-content-between
    h2
      span: a(href="/"): i.bi.bi-arrow-left-circle
      |  #{accountName} Summary
    a(href="/Logout").btn.btn-outline-primary.btn-sm.btn-header Logout
  hr
  div.d-flex.justify-content-between
    h4 Transactions
    a(href="/AccountDetails/" + accountId + "/CreateTransaction").btn.btn-primary.btn-sm: span.bi.bi-plus New Transaction
  table.table.table-striped.table-responsive.table-hover.table-sm
    thead
      tr
        th Date
        th Description
        th: a(href="/TransactionCategories") Category
        th Amount
    tbody
      if transactions
        each tx in transactions
          tr
            td: a(href="/AccountDetails/"+accountId+"/ModifyTransaction/"+tx.id) #{tx.date}
            td: strong #{tx.description}
            td #{tx.category}
            td $#{tx.amount}
